export default function VehicleHero({title,subtitle,image,price,availability}:{title:string;subtitle:string;image?:string;price?:string;availability?:string}){return <section className="hero vehicle-hero"><div className="container hero-grid"><div><span className="eyebrow" style={{color:'#bcd4ee'}}>Volkswagen</span><h1>{title}</h1><p>{subtitle}</p>{price&&<div className="hero-price">{price}</div>}{availability&&<div className="hero-availability">{availability}</div>}<a className="btn btn-primary" href="#proposta">Solicitar proposta</a></div><div className="hero-art" style={image?{backgroundImage:`url(${image})`,backgroundSize:'cover',backgroundPosition:'center'}:undefined}/></div></section>}
