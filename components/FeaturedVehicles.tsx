import {getVehicles} from '@/lib/vehicles';
export default async function FeaturedVehicles(){const cars=(await getVehicles()).filter(v=>v.featured);return <section className="section vehicles" id="modelos"><div className="container"><span className="eyebrow">Modelos</span><h2>Volkswagen em destaque.</h2><div className="vehicle-grid">{cars.map(c=><article className="vehicle-card" key={c.slug}><div className="vehicle-art" style={c.hero_image?{backgroundImage:`url(${c.hero_image})`,backgroundSize:'cover',backgroundPosition:'center'}:undefined}/><div className="vehicle-info"><h3>{c.title}</h3><p>{c.subtitle}</p>{c.price&&<strong className="vehicle-price">{c.price}</strong>}<a className="text-link" href={`/modelos/${c.slug}`}>Conhecer modelo →</a></div></article>)}</div></div></section>}
